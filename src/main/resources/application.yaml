spring:
  datasource:
    # source : ??? ?? ? ??? ???? ????? ?.
    # ???, ???? => ??? ?? ? ??. ??? ?? ? ???
    # datasource : ???? ?? ? ?? ???? ?.
    url: jdbc:mysql://localhost:3306/biddb
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # ??? ??? ? ?? ???? (????? ??? ?)
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 60000
      connection-timeout: 20000
      pool-name: HikariCP


  jpa:
    # ???? ??? ? ??? ???? ??, ??? ??? ???? ???
    hibernate:
      ddl-auto: create
      # (Data Definition Language)
      # ???? ??? ???? ??.
      # ???? ???? ?? ???? ???? create ??? update ? ???? ?.
    show-sql: true
    # ?? ??? ???? ?. true ? ?? ???
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        # ???? ??? ?? ???? ???



# Optional: To enable logging of SQL statements
logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace
